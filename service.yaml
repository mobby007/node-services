apiVersion: v1
kind: Service
metadata:
  name: node-service
  labels:
    app: node-service
spec:
  type: ClusterIP
  selector:
    app: node-service
  ports:
    - protocol: TCP
      port: 3000        # Port exposed within the cluster
      targetPort: 3000  # Port exposed by the Node.js container
