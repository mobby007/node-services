apiVersion: v1
kind: Service
metadata:
  name: node-service
  labels:
    app: node-service
spec:
  selector:             # ✅ This line was fine, but YAML indenting needs to be perfect
    app: node-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3000        # Port exposed inside the cluster
      targetPort: 3000  # Port exposed by the Node.js container

